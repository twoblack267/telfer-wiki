node_modules/
dist/
.cache/
.env
.DS_Store
public/audit-history/
# public/research/ — removing so the 1986 Telfer Reunion Booklet PDF deploys
scripts/redirect-log.json
src/data/people.public.json

apply-prune.py
*.full-backup
*.bak
runtime/

src/data/_pre-clean-2026-08-10/
scripts/sameas-candidates.json

# Personal screenshots — never publish to the public wiki
screenshot-evidence/

# Resolver-coverage + escalation RUNTIME STATE (emitted by
# scripts/validate-resolver-coverage.mjs / scripts/ci-escalate.py).
# Artifacts, not source — they change every build and must never be committed.
.resolver-coverage.json
.ci-escalation.json
__pycache__/
*.pyc
